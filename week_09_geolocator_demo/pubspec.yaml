name: week_09_geolocator_demo
description: 'A new Flutter project.'
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^12.0.0

# The following is added to address https://github.com/Baseflow/flutter-geolocator/issues/1515
dependency_overrides:
  geolocator_android: 4.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
